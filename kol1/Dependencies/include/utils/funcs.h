#ifndef COMMON_FUNCS_
#define COMMON_FUNCS_

#include <utils/single_linked_list.h>

#include<vector>

const size_t kFactorialMax = 20;

std::vector<size_t> n_factorial(size_t);
std::vector<size_t> n_factorial(size_t, size_t);
std::vector<double> remove_duplicates(std::vector<double>);

#endif